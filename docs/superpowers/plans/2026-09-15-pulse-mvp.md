# Pulse MVP 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建本地优先的项目管理工具 pulse——CLI（人）+ MCP（编码 agent）双入口操作同一 SQLite，任务/版本数据与共享飞书多维表格双向同步，报表可发布到飞书文档。

**Architecture:** 单 Go 二进制。`internal/store`（SQLite 存储 + activity 审计）为唯一写路径；`internal/cli`（cobra）与 `internal/mcpserver`（官方 go-sdk stdio）是它的两个薄封装；`internal/rules` 纯函数风险引擎；`internal/reports` 生成 HTML/Markdown；`internal/feishu` 是可选项（sync 双向同步多维表格、publish 单向写文档），用 adapter 接口隔离官方 SDK 以便 httptest 契约测试。

**Tech Stack:** Go 1.25、`modernc.org/sqlite`（纯 Go 无 cgo）、`spf13/cobra`、`modelcontextprotocol/go-sdk`（stdio + InMemoryTransports 测试）、`larksuite/oapi-sdk-go/v3`、标准库 `html/template`。

**Spec:** `docs/superpowers/specs/2026-09-14-pulse-mvp-design.md`（本文所有决策以其为准）

## Global Constraints

- Go 1.25；模块名 `github.com/zhangyi/pulse`
- SQLite 驱动只许 `modernc.org/sqlite`（禁止 mattn/go-sqlite3，cgo 不可接受）
- 数据目录：默认 `~/.pulse/pulse.db`，测试与 CI 一律用 `PULSE_HOME` 环境变量重定向到临时目录
- 日期一律 `2006-01-02` 文本；时间戳一律 UTC `2006-01-02 15:04:05`
- **所有写操作必须落 `activity` 表**（actor + actor_type + on_behalf_of 归因）——这是 core 的不变式
- 状态 `done → 非 done` 的流转在 activity 中记 `action=reopen`，其余状态变更记 `update_status`
- core 与 rules/reports **零网络调用、零 LLM**；只有 `internal/feishu` 可以发 HTTP
- MCP server 的 actor 来自启动时 `PULSE_ACTOR` 环境变量（自动注册为 `type=agent` 成员），工具参数 `delegated_by` 为可选委托人
- 飞书：tenant_access_token；429 指数退避最多 5 次；每 base 串行写；sync/publish 幂等可重跑；未配置飞书时本地功能完全可用
- 硬编码中文文案（CLI 帮助、报表标题、activity 动作名）保持本计划给出的措辞，便于 golden 测试稳定

## File Map

```
pulse/
├── go.mod                          # module github.com/zhangyi/pulse
├── main.go                         # 调用 cli.Execute()
├── internal/
│   ├── config/config.go            # ~/.pulse/config.yaml；PULSE_FEISHU_APP_SECRET 覆盖
│   ├── model/model.go              # Member/Project/Task/Version/Dependency/Activity 纯结构
│   ├── store/store.go              # Open/Migrate（含 sync_state 表）
│   ├── store/members.go            # GetOrCreateMember/ListMembers
│   ├── store/projects.go           # CreateProject/GetProjectByKey/ListProjects/SaveProject
│   ├── store/tasks.go              # CreateTask/UpdateTask(reopen 语义)/ListTasks/SoftDeleteTask
│   ├── store/versions.go           # CreateVersion/ListVersions/GetVersion
│   ├── store/deps.go               # AddDependency/ListDependencies
│   ├── store/activity.go           # LogActivity/ActivitiesInWindow
│   ├── actor/actor.go              # CLI/MCP 的 actor 解析（get-or-create）
│   ├── rules/rules.go              # 风险引擎 + Workloads（纯函数）
│   ├── reports/gantt.go            # 甘特 HTML
│   ├── reports/workload.go         # 人力 HTML
│   ├── reports/release.go          # 版本 HTML
│   ├── reports/weekly.go           # 周报 Markdown（基于 activity）
│   ├── mcpserver/server.go         # MCP tools（全部经 store，复用 actor 逻辑）
│   ├── cli/                        # cobra 命令（root/init/project/member/task/version/report/sync/feishu/mcp）
│   └── feishu/
│       ├── api.go                  # bitableAPI/docxAPI 接口 + SDK 适配器（可 mock）
│       ├── client.go               # token 缓存/刷新、429 退避、每 base 串行写队列
│       ├── bind.go                 # 创建 base+表+甘特视图+文档
│       ├── mapping.go              # SQLite↔Bitable 字段映射 + contentHash
│       ├── sync.go                 # push/pull/自回声抑制/软删
│       ├── publish.go              # 报表块写入 docx
│       └── autopush.go             # 写命令后的 best-effort 自动 push
└── README.md
```

---

### Task 1: 脚手架 + config 模块

**Files:**
- Create: `go.mod`、`main.go`、`internal/config/config.go`、`internal/config/config_test.go`

**Interfaces:**
- Produces: `config.Config{DefaultActor string; Feishu config.Feishu{AppID, AppSecret string}; Sync config.Sync{StaleMinutes int}}`；`config.Load(path string) (*Config, error)`（文件不存在返回默认值）；`config.DefaultPath() string`（`$PULSE_HOME/config.yaml`，无 PULSE_HOME 时 `~/.pulse/config.yaml`）

- [ ] **Step 1: 初始化模块与目录**

```bash
cd /Users/demo/my_project/lacus/pulse
go mod init github.com/zhangyi/pulse
mkdir -p internal/config internal/model internal/store internal/actor internal/rules internal/reports internal/mcpserver internal/cli internal/feishu
go get github.com/spf13/cobra@latest modernc.org/sqlite@latest
```

- [ ] **Step 2: 写失败测试** `internal/config/config_test.go`

```go
package config

import (
	"os"
	"path/filepath"
	"testing"
)

func TestLoadDefaultsWhenFileMissing(t *testing.T) {
	c, err := Load(filepath.Join(t.TempDir(), "config.yaml"))
	if err != nil {
		t.Fatal(err)
	}
	if c.DefaultActor != "" || c.Sync.StaleMinutes != 10 || c.Feishu.AppID != "" {
		t.Fatalf("unexpected defaults: %+v", c)
	}
}

func TestLoadFromFileAndSecretEnv(t *testing.T) {
	dir := t.TempDir()
	os.WriteFile(filepath.Join(dir, "config.yaml"), []byte(
		"default_actor: zhangyi\nfeishu:\n  app_id: cli_a1\n  app_secret: fromfile\nsync:\n  stale_minutes: 30\n"), 0o600)
	t.Setenv("PULSE_FEISHU_APP_SECRET", "fromenv")
	c, err := Load(filepath.Join(dir, "config.yaml"))
	if err != nil {
		t.Fatal(err)
	}
	if c.DefaultActor != "zhangyi" || c.Feishu.AppID != "cli_a1" || c.Sync.StaleMinutes != 30 {
		t.Fatalf("got %+v", c)
	}
	if c.Feishu.AppSecret != "fromenv" {
		t.Fatalf("env must override file, got %q", c.Feishu.AppSecret)
	}
}

func TestDefaultPathHonorsPulseHome(t *testing.T) {
	t.Setenv("PULSE_HOME", "/tmp/pulse-x")
	if got := DefaultPath(); got != "/tmp/pulse-x/config.yaml" {
		t.Fatalf("got %q", got)
	}
}
```

- [ ] **Step 3: 运行确认失败** — `go test ./internal/config/`，预期：编译错误 `Load` 未定义

- [ ] **Step 4: 实现** `internal/config/config.go`

```go
package config

import (
	"os"
	"path/filepath"

	"gopkg.in/yaml.v3"
)

type Feishu struct {
	AppID     string `yaml:"app_id"`
	AppSecret string `yaml:"app_secret"`
}

type Sync struct {
	StaleMinutes int `yaml:"stale_minutes"`
}

type Config struct {
	DefaultActor string `yaml:"default_actor"`
	Feishu       Feishu `yaml:"feishu"`
	Sync         Sync   `yaml:"sync"`
}

func DefaultPath() string {
	if h := os.Getenv("PULSE_HOME"); h != "" {
		return filepath.Join(h, "config.yaml")
	}
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".pulse", "config.yaml")
}

func DBPath() string {
	if h := os.Getenv("PULSE_HOME"); h != "" {
		return filepath.Join(h, "pulse.db")
	}
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".pulse", "pulse.db")
}

// Load 读取配置；文件不存在时返回默认值。环境变量 PULSE_FEISHU_APP_SECRET 优先于文件。
func Load(path string) (*Config, error) {
	c := &Config{Sync: Sync{StaleMinutes: 10}}
	data, err := os.ReadFile(path)
	if err == nil {
		if err := yaml.Unmarshal(data, c); err != nil {
			return nil, err
		}
	} else if !os.IsNotExist(err) {
		return nil, err
	}
	if c.Sync.StaleMinutes <= 0 {
		c.Sync.StaleMinutes = 10
	}
	if s := os.Getenv("PULSE_FEISHU_APP_SECRET"); s != "" {
		c.Feishu.AppSecret = s
	}
	return c, nil
}
```

补充依赖：`go get gopkg.in/yaml.v3@latest`

- [ ] **Step 5: 运行测试通过** — `go test ./internal/config/`，预期 PASS

- [ ] **Step 6: 写 main.go 并提交**

`main.go`：

```go
package main

import "github.com/zhangyi/pulse/internal/cli"

func main() { cli.Execute() }
```

`internal/cli/root.go`（暂为最小骨架，后续任务扩展）：

```go
package cli

import "github.com/spf13/cobra"

func newRootCmd() *cobra.Command {
	return &cobra.Command{Use: "pulse", Short: "本地优先的项目管理核心"}
}

// Execute 是 main 的唯一入口。
func Execute() {
	if err := newRootCmd().Execute(); err != nil {
		os.Exit(1)
	}
}
```

（`os` 需 import。）提交：

```bash
git add -A && git commit -m "feat: scaffold module, cobra root, config with env secret override"
```

---

### Task 2: store 打开与迁移（含 sync_state）

**Files:**
- Create: `internal/model/model.go`、`internal/store/store.go`、`internal/store/store_test.go`

**Interfaces:**
- Produces: `store.Open(path string) (*Store, error)`（`":memory:"` 合法）、`(*Store).Close() error`；model 包六个体：

```go
type Member struct {
	ID        int64
	Name      string
	Type      string // human | agent
	Capacity  float64
	Notes     string
	CreatedAt string
}
type Project struct {
	ID                        int64
	Key, Name, Description, Status string
	FeishuDocToken            string
	FeishuBitableAppToken     string
	FeishuTaskTableID         string // 计划细化：spec 的单 table_id 拆为 task/version 两个
	FeishuVersionTableID      string
	CreatedAt                 string
}
type Task struct {
	ID int64
	ProjectID              int64
	Title, Description     string
	AssigneeID             int64 // 0 = 无人
	Status                 string
	Priority               int
	EstimateDays           float64
	StartDate, DueDate     string
	VersionID              int64 // 0 = 无版本
	BitableRecordID        string
	BitableSyncedHash      string
	Archived               bool
	StatusChangedAt        string
	CreatedAt, UpdatedAt   string
}
type Version struct {
	ID int64
	ProjectID      int64
	Name           string
	TargetDate     string
	Status         string // planned | in_dev | released | shipped
	Notes          string
	BitableRecordID, BitableSyncedHash string
}
type Dependency struct {
	ID, TaskID, DependsOnTaskID int64
	Type string
}
type Activity struct {
	ID        int64
	ProjectID int64
	ActorID   int64
	ActorType string
	OnBehalfOf int64 // 0 = 无
	Action    string
	EntityType string
	EntityID  int64
	Detail    string // JSON
	CreatedAt string
}
```

- [ ] **Step 1: 写失败测试** `internal/store/store_test.go`

```go
package store

import (
	"testing"

	_ "github.com/zhangyi/pulse/internal/model"
)

func openTest(t *testing.T) *Store {
	t.Helper()
	s, err := Open(":memory:")
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { s.Close() })
	return s
}

func TestMigrateIsIdempotent(t *testing.T) {
	s := openTest(t)
	if err := s.migrate(); err != nil {
		t.Fatal(err)
	}
	if err := s.migrate(); err != nil { // 二次执行不得报错
		t.Fatal(err)
	}
}

func TestOpenBadPathFails(t *testing.T) {
	if _, err := Open("/nonexistent-dir-x/pulse.db"); err == nil {
		t.Fatal("expected error for unwritable path")
	}
}
```

- [ ] **Step 2: 运行确认失败** — `go test ./internal/store/`（编译错误）

- [ ] **Step 3: 实现** `internal/store/store.go`

```go
package store

import (
	"database/sql"
	"fmt"

	_ "modernc.org/sqlite"
)

type Store struct{ db *sql.DB }

const schema = `
CREATE TABLE IF NOT EXISTS members (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE,
  type TEXT NOT NULL DEFAULT 'human' CHECK(type IN ('human','agent')),
  capacity_days_per_week REAL NOT NULL DEFAULT 5,
  notes TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now'))
);
CREATE TABLE IF NOT EXISTS projects (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL DEFAULT '',
  description TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'active',
  feishu_doc_token TEXT NOT NULL DEFAULT '',
  feishu_bitable_app_token TEXT NOT NULL DEFAULT '',
  feishu_task_table_id TEXT NOT NULL DEFAULT '',
  feishu_version_table_id TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now'))
);
CREATE TABLE IF NOT EXISTS versions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER NOT NULL REFERENCES projects(id),
  name TEXT NOT NULL,
  target_date TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'planned' CHECK(status IN ('planned','in_dev','released','shipped')),
  notes TEXT NOT NULL DEFAULT '',
  bitable_record_id TEXT NOT NULL DEFAULT '',
  bitable_synced_hash TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now')),
  UNIQUE(project_id, name)
);
CREATE TABLE IF NOT EXISTS tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER NOT NULL REFERENCES projects(id),
  title TEXT NOT NULL,
  description TEXT NOT NULL DEFAULT '',
  assignee_id INTEGER REFERENCES members(id),
  status TEXT NOT NULL DEFAULT 'todo' CHECK(status IN ('backlog','todo','in_progress','blocked','done')),
  priority INTEGER NOT NULL DEFAULT 3,
  estimate_days REAL NOT NULL DEFAULT 0,
  start_date TEXT NOT NULL DEFAULT '',
  due_date TEXT NOT NULL DEFAULT '',
  version_id INTEGER REFERENCES versions(id),
  requirement_id INTEGER,
  bitable_record_id TEXT NOT NULL DEFAULT '',
  bitable_synced_hash TEXT NOT NULL DEFAULT '',
  archived INTEGER NOT NULL DEFAULT 0,
  status_changed_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now')),
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now')),
  updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now'))
);
CREATE TABLE IF NOT EXISTS dependencies (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id INTEGER NOT NULL REFERENCES tasks(id),
  depends_on_task_id INTEGER NOT NULL REFERENCES tasks(id),
  type TEXT NOT NULL DEFAULT 'FS'
);
CREATE TABLE IF NOT EXISTS activity (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER REFERENCES projects(id),
  actor_id INTEGER NOT NULL REFERENCES members(id),
  actor_type TEXT NOT NULL,
  on_behalf_of INTEGER REFERENCES members(id),
  action TEXT NOT NULL,
  entity_type TEXT NOT NULL,
  entity_id INTEGER NOT NULL,
  detail TEXT NOT NULL DEFAULT '{}',
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%d %H:%M:%S','now'))
);
CREATE TABLE IF NOT EXISTS sync_state (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL
);
`

func Open(path string) (*Store, error) {
	db, err := sql.Open("sqlite", path+"?_pragma=foreign_keys(1)&_pragma=busy_timeout(5000)")
	if err != nil {
		return nil, err
	}
	s := &Store{db: db}
	if err := s.migrate(); err != nil {
		db.Close()
		return nil, fmt.Errorf("migrate: %w", err)
	}
	return s, nil
}

func (s *Store) migrate() error {
	_, err := s.db.Exec(schema)
	return err
}

func (s *Store) Close() error { return s.db.Close() }
```

创建 `internal/model/model.go`，写入 Task 2 Interfaces 中列出的六个结构体（纯字段声明，`package model`）。

- [ ] **Step 4: 测试通过** — `go test ./internal/store/ ./internal/model/`，预期 PASS

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: store open/migrate with full schema and sync_state"`

---

### Task 3: members + activity + actor 解析

**Files:**
- Create: `internal/store/members.go`、`internal/store/activity.go`、`internal/actor/actor.go`
- Test: `internal/store/members_test.go`、`internal/actor/actor_test.go`

**Interfaces:**
- Consumes: `store.Open`
- Produces:
  - `(*Store).GetOrCreateMember(name, typ string) (model.Member, error)`（同名存在则校验 type 一致后返回）
  - `(*Store).ListMembers() ([]model.Member, error)`
  - `(*Store).LogActivity(a model.Activity) error`（CreatedAt 留空则由 DB 填）
  - `(*Store).ActivitiesInWindow(projectID int64, from, to time.Time) ([]model.Activity, error)`（含 from、不含 to）
  - `actor.Resolve(s *store.Store, defaultActor, agentName, delegatedBy string) (actor model.Member, behalf *model.Member, err error)`：agentName != "" 时 actor 为 `type=agent` 成员（get-or-create）；否则 actor 为 defaultActor（human，get-or-create，defaultActor 为空时报错）；delegatedBy 非空时解析为 behalf

- [ ] **Step 1: 写失败测试** `internal/store/members_test.go`

```go
package store

import (
	"testing"

	"github.com/zhangyi/pulse/internal/model"
)

func TestGetOrCreateMemberIdempotent(t *testing.T) {
	s := openTest(t)
	m1, err := s.GetOrCreateMember("zhangyi", "human")
	if err != nil {
		t.Fatal(err)
	}
	m2, err := s.GetOrCreateMember("zhangyi", "human")
	if err != nil || m1.ID != m2.ID {
		t.Fatalf("must reuse same member: %v %v", m1, m2)
	}
	if _, err := s.GetOrCreateMember("zhangyi", "agent"); err == nil {
		t.Fatal("type conflict must error")
	}
}

func TestActivityWindow(t *testing.T) {
	s := openTest(t)
	actor, _ := s.GetOrCreateMember("a", "human")
	if err := s.LogActivity(model.Activity{ProjectID: 1, ActorID: actor.ID, ActorType: "human",
		Action: "create", EntityType: "task", EntityID: 1}); err != nil {
		t.Fatal(err)
	}
	acts, err := s.ActivitiesInWindow(1, time.Now().Add(-time.Hour), time.Now().Add(time.Hour))
	if err != nil || len(acts) != 1 {
		t.Fatalf("got %v, %v", acts, err)
	}
}
```

- [ ] **Step 2: 运行确认失败**，然后实现 `members.go` / `activity.go`（标准 INSERT OR IGNORE + SELECT、窗口查询 `created_at >= ? AND created_at < ?`，时间格式 `datetime(...)` 与 schema 默认值一致）

- [ ] **Step 3: actor 失败测试** `internal/actor/actor_test.go`

```go
package actor

import (
	"testing"

	"github.com/zhangyi/pulse/internal/store"
)

func TestResolveHumanDefault(t *testing.T) {
	s, _ := store.Open(":memory:")
	defer s.Close()
	a, behalf, err := Resolve(s, "zhangyi", "", "")
	if err != nil || a.Name != "zhangyi" || a.Type != "human" || behalf != nil {
		t.Fatalf("got %v %v %v", a, behalf, err)
	}
}

func TestResolveAgentWithDelegatedBy(t *testing.T) {
	s, _ := store.Open(":memory:")
	defer s.Close()
	a, behalf, err := Resolve(s, "zhangyi", "codex", "zhangyi")
	if err != nil || a.Type != "agent" || a.Name != "codex" || behalf == nil || behalf.Name != "zhangyi" {
		t.Fatalf("got %v %v %v", a, behalf, err)
	}
}

func TestResolveNoDefaultErrors(t *testing.T) {
	s, _ := store.Open(":memory:")
	defer s.Close()
	if _, _, err := Resolve(s, "", "", ""); err == nil {
		t.Fatal("empty default actor must error")
	}
}
```

- [ ] **Step 4: 实现 `internal/actor/actor.go`**（组合 `GetOrCreateMember`；agent 分支先确保 defaultActor 存在以便 delegatedBy 解析，delegatedBy 解析失败即报错）并运行 `go test ./internal/store/ ./internal/actor/` 通过

- [ ] **Step 5: Commit** — `git commit -am "feat: members, activity log, actor resolution"`

---

### Task 4: projects store + CLI（init/list/project）

**Files:**
- Create: `internal/store/projects.go`、`internal/cli/project.go`
- Modify: `internal/cli/root.go`
- Test: `internal/store/projects_test.go`、`internal/cli/cli_test.go`

**Interfaces:**
- Consumes: `store.Open`、`actor.Resolve`、`config.Load/DefaultPath/DBPath`
- Produces:
  - `(*Store).CreateProject(key, name, desc string) (model.Project, error)`；`GetProjectByKey(key string) (model.Project, bool, error)`；`ListProjects() ([]model.Project, error)`；`SaveProject(p model.Project) error`（全量 UPDATE，供 feishu bind 写回 token）
  - CLI：`pulse init <key>`、`pulse project list`、`pulse member add <name> --type human|agent --capacity 5`、`pulse member list`
  - `internal/cli` 内部助手（后续任务复用）：`openApp() (*store.Store, *config.Config, error)`、`resolveActor(s, cfg, cmd) (model.Member, *model.Member, error)`

- [ ] **Step 1: store 失败测试**（`projects_test.go`：创建/重复 key 报错/按 key 查询/SaveProject 回读）+ **Step 2: 确认失败** + **Step 3: 实现 projects.go**

- [ ] **Step 4: CLI 测试** `internal/cli/cli_test.go`

```go
package cli

import (
	"bytes"
	"path/filepath"
	"testing"

	"github.com/zhangyi/pulse/internal/config"
	"github.com/zhangyi/pulse/internal/store"
)

func testEnv(t *testing.T) (*store.Store, *config.Config, *bytes.Buffer) {
	t.Helper()
	dir := t.TempDir()
	t.Setenv("PULSE_HOME", dir)
	cfgPath := filepath.Join(dir, "config.yaml")
	os.WriteFile(cfgPath, []byte("default_actor: tester\n"), 0o600)
	cfg, err := config.Load(cfgPath)
	if err != nil {
		t.Fatal(err)
	}
	s, err := store.Open(filepath.Join(dir, "pulse.db"))
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { s.Close() })
	return s, cfg, &bytes.Buffer{}
}
```

root.go 重构为可注入（测试用 `newRootCmd().SetArgs([]string{"init","demo"}); Execute()`，输出走 `SetOut(buf)`）。init 命令行为：`pulse init demo` → 打印 `项目已创建: demo (id=N)`；重复 init → 退出码 1 且 stderr 报 `项目已存在: demo`。

- [ ] **Step 5: 测试通过后 Commit** — `git commit -am "feat: project store + init/list/member commands"`

---

### Task 5: tasks store（CRUD + reopen 语义）+ task CLI

**Files:**
- Create: `internal/store/tasks.go`、`internal/cli/task.go`
- Test: `internal/store/tasks_test.go`

**Interfaces:**
- Produces:

```go
type TaskFilter struct {
	AssigneeID      int64  // 0 = 不过滤
	Status          string // "" = 不过滤
	VersionID       int64
	OverdueOnly     bool   // due_date < today AND status != 'done'
	IncludeArchived bool
}

func (s *Store) CreateTask(t model.Task, actor model.Member, behalf *model.Member) (model.Task, error)
func (s *Store) GetTask(id int64) (model.Task, bool, error)
func (s *Store) ListTasks(projectID int64, f TaskFilter) ([]model.Task, error)
func (s *Store) UpdateTask(id int64, ch TaskChanges, actor model.Member, behalf *model.Member) (model.Task, error)
func (s *Store) SoftDeleteTask(id int64, actor model.Member, behalf *model.Member) error

type TaskChanges struct {
	Title, Description, Status, StartDate, DueDate *string
	AssigneeID, VersionID, Priority                *int64
	EstimateDays                                   *float64
}
```

- **reopen 不变式（核心测试点）**：`UpdateTask` 把 status 从 `done` 改为其他值 → activity 记 `action="reopen"`；否则记 `action="update_status"`；status 变更同时刷新 `status_changed_at`；任何变更刷新 `updated_at`
- 每次变更的 activity detail JSON：`{"field":"status","from":"done","to":"todo"}`；CreateTask 记 `action="create"`
- CLI：`pulse task add <title> --project demo [--assignee NAME] [--status todo] [--priority 3] [--estimate 2] [--start 2026-09-15] [--due 2026-09-20] [--version v1]`；`pulse task list --project demo [--mine] [--status] [--overdue]`（表格输出列：ID/标题/状态/负责人/截止/版本）；`pulse task update <id> [--status] [--assignee] ...`；`pulse task rm <id>`

- [ ] **Step 1: 写失败测试**（至少覆盖）：create 落 activity；update status `todo→done` 记 update_status 且刷新 status_changed_at；`done→todo` 记 reopen；filter：assignee/status/overdue/include_archived 各一例；软删后默认列表不可见
- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现 tasks.go**（UpdateTask 在一个事务里：读旧行→拼 UPDATE→写 activity→提交）
- [ ] **Step 4: 实现 task.go CLI**（`--assignee` 按名字解析成员，get-or-create human）
- [ ] **Step 5: `go test ./...` 通过**
- [ ] **Step 6: Commit** — `git commit -am "feat: task store with reopen semantics + task cli"`

---

### Task 6: dependencies + versions（store + CLI）

**Files:**
- Create: `internal/store/deps.go`、`internal/store/versions.go`、`internal/cli/version.go`
- Modify: `internal/cli/task.go`（加 `pulse task dep <id> --on <taskID>`）
- Test: `internal/store/deps_versions_test.go`

**Interfaces:**
- Produces:
  - `(*Store).AddDependency(taskID, dependsOnID int64, actor model.Member, behalf *model.Member) error`（自依赖报错；重复报错；activity `action="add_dependency"`）
  - `(*Store).ListDependencies(projectID int64) ([]model.Dependency, error)`（JOIN tasks 限定项目）
  - `(*Store).CreateVersion(v model.Version, actor, behalf) (model.Version, error)`；`ListVersions(projectID)`；`GetVersion(id)`；`UpdateVersion(id int64, ch VersionChanges, actor, behalf) (model.Version, error)`
- CLI：`pulse version add <name> --project demo [--target 2026-10-01]`、`pulse version list --project demo`、`pulse version update <id> --status in_dev|released|shipped [--target]`

- [ ] **Step 1: 失败测试**（自依赖拒绝、重复拒绝、versions 唯一名约束、activity 断言）
- [ ] **Step 2: 确认失败 → Step 3: 实现 → Step 4: `go test ./...` 通过 → Step 5: Commit** `git commit -am "feat: dependencies and versions"`

---

### Task 7: rules 风险引擎（纯函数）

**Files:**
- Create: `internal/rules/rules.go`
- Test: `internal/rules/rules_test.go`

**Interfaces:**
- Consumes: `store.ListTasks`、`store.ListDependencies`、`store.ListMembers`
- Produces:

```go
type Risk struct {
	Kind    string // overdue | blocked | unassigned | inversion | overload
	Level   string // high | medium
	Title   string // 人读摘要，如 "#3 登录接口 逾期 2 天"
	Detail  string
}

// Evaluate 返回项目的全部风险。blockedDays: blocked 判定阈值；now 传入便于测试。
func Evaluate(s *store.Store, projectID int64, now time.Time, blockedDays int) ([]Risk, error)

type Workload struct {
	Member     model.Member
	LoadDays   float64 // 未来 14 天内到期未完成任务的 estimate 之和
	CapacityDays float64
	LoadRate   float64 // LoadDays / CapacityDays；Capacity=0 时为 0
}

func Workloads(s *store.Store, projectID int64, now time.Time) ([]Workload, error)
```

- 规则（全部排除 `archived`）：overdue=`due<now日期 && status!=done`；blocked=`status=blocked && now-status_changed_at>blockedDays 天`；unassigned=`assignee=0 && version_id!=0`；inversion=对每条 `A dep B`，两者都有 due 且 `B.due > A.due`；overload=`Workload.LoadRate > 1.0`（Level=high）
- [ ] **Step 1: 表驱动失败测试**（每规则至少 1 正 1 反例；overload 用 capacity=1、两条 estimate=2 期限 3 天内的任务验证 rate=2.0）
- [ ] **Step 2: 确认失败 → Step 3: 实现 → Step 4: 通过 → Step 5: Commit** `git commit -am "feat: risk rules engine (pure functions)"`

---

### Task 8: 报表四件套 + report CLI

**Files:**
- Create: `internal/reports/gantt.go`、`internal/reports/workload.go`、`internal/reports/release.go`、`internal/reports/weekly.go`、`internal/reports/templates/*.html`（`embed`）、`internal/cli/report.go`
- Test: `internal/reports/reports_test.go`（golden 文件）

**Interfaces:**
- Consumes: store 全部读接口、rules.Evaluate/Workloads
- Produces:

```go
func GanttHTML(s *store.Store, projectID int64) ([]byte, error)      // 有 start+due 的任务条（CSS 绝对定位条形），依赖箭头省略为条下文字 "依赖 #id"，版本分组
func WorkloadHTML(s *store.Store, projectID int64, now time.Time) ([]byte, error)
func VersionsHTML(s *store.Store, projectID int64, now time.Time) ([]byte, error) // 含每版本风险摘要
func WeeklyMarkdown(s *store.Store, projectID int64, now time.Time) ([]byte, error)
```

- weekly 语义（spec §4/§4 计算口径）：窗口=ISO 周（now 所在周一 00:00 至下周一）；"本周完成"=窗口内 `action IN ('update_status','reopen')` 且 detail `to=="done"` 的 activity 聚合到任务；"进行中"=status=in_progress；"风险清单"=rules.Evaluate 全量；"未排期"=无 due 未完成任务计数；"agent 贡献"=窗口内 actor_type='agent' 的 activity 计数按 actor 分组
- HTML 模板为自包含单文件（内联 CSS，无外链），golden 测试：`golden/` 目录存期望输出，`-update` flag 重新生成

- [ ] **Step 1: weekly 的失败测试**（构造 activity 断言"本周完成"与 agent 贡献统计）
- [ ] **Step 2: 确认失败 → Step 3: 实现 weekly.go → Step 4: 通过**
- [ ] **Step 5: 三个 HTML 报表实现 + golden 测试通过**
- [ ] **Step 6: report CLI**：`pulse report gantt|workload|versions|weekly --project demo [--out FILE]`（默认 stdout）
- [ ] **Step 7: `go test ./...` → Commit** `git commit -am "feat: four reports (gantt/workload/versions/weekly) with golden tests"`

---

### Task 9: MCP server + `pulse mcp`

**Files:**
- Create: `internal/mcpserver/server.go`、`internal/mcpserver/server_test.go`
- Modify: `internal/cli/mcp.go`（新增 `pulse mcp` 命令）

**Interfaces:**
- Consumes: store 全部、`actor.Resolve`
- Produces: `mcpserver.New(s *store.Store, agentName string) *mcp.Server`（`github.com/modelcontextprotocol/go-sdk/mcp`）。工具清单（与 spec §5.2 一致，全部返回 JSON 字符串）：

| Tool | 入参（JSON schema 字段） | 行为 |
|---|---|---|
| `list_projects` | - | 全部项目 |
| `get_project_status` | `project`(必填) | 项目元数据 + 未完成任务数 + 风险摘要（调 rules.Evaluate） |
| `list_tasks` | `project`必填, `assignee?`, `status?`, `version?`, `overdue?` | TaskFilter 映射；`assignee="me"` 解析为 agent 自己 |
| `add_task` | `project`必填, `title`必填, `assignee?`, `status?`, `priority?`, `estimate_days?`, `start?`, `due?`, `version?`, `delegated_by?` | 经 actor.Resolve(delegated_by) 后 CreateTask |
| `update_task` | `id`必填, 其余可选同上, `delegated_by?` | UpdateTask（reopen 语义自动生效） |
| `add_dependency` | `task_id`必填, `depends_on_task_id`必填, `delegated_by?` | |
| `list_versions`/`add_version`/`update_version` | 同 CLI 语义 | |
| `list_members`/`get_workload` | `project`必填(get_workload) | rules.Workloads |
| `publish_feishu` | `project`必填, `report`(weekly|versions|all) | 调 feishu.PublishReport；未配置飞书返回引导错误文本 |

- 每个 tool 的 description 必须含这句（spec §5.2）：`更新任务前先 list_tasks 确认 id`
- actor：`New` 的 `agentName` 参数来自 `pulse mcp` 命令读取的 `PULSE_ACTOR` 环境变量；为空时报错退出并提示"启动 MCP server 需设置 PULSE_ACTOR"

- [ ] **Step 1: 写失败测试**（用 `mcp.NewInMemoryTransports()` + `mcp.Start`（client/server 各起一端），断言：`add_task` 后 `list_tasks` 能看到；`PULSE_ACTOR` 对应的 agent 成员自动创建；`update_task` 把任务改 done 后 activity 中有 update_status）

```go
func TestAddAndListTasks(t *testing.T) {
	s := testStore(t)
	ct, st := mcp.NewInMemoryTransports()
	srv := mcp.NewServer(&mcp.Implementation{Name: "pulse"}, nil)
	mcpserver.Register(srv, s) // New 内部也调 Register；测试直接用 Register 便于注入
	sc, _ := ct.Connect(context.Background())
	srv.Run(context.Background(), st, nil) // 实际 API 以 go-sdk 为准：若为 Serve 聚合函数则整体替换
	resp, err := sc.CallTool(ctx, &mcp.CallToolParams{Name: "add_task", Arguments: map[string]any{
		"project": "demo", "title": "t1"}})
	// 断言 resp 无 error；再 list_tasks 断言包含 t1
}
```

注意：go-sdk 具体 API（`Server.Run`/`Serve`、`ClientTransport.Connect` 签名）以 `go doc github.com/modelcontextprotocol/go-sdk/mcp` 实测为准——测试代码整体结构不变，只对齐调用名。**这不是占位符**：工具注册、参数解析、actor 注入、断言逻辑必须按本表完整实现。
- [ ] **Step 2: 确认失败 → Step 3: 实现全部 10 个工具 → Step 4: 测试通过 → Step 5: 实现 `pulse mcp` 命令（stdio 传输）→ Step 6: Commit** `git commit -am "feat: mcp server exposing core as agent tools"`

---

### Task 10: feishu 客户端基座（token/退避/串行队列/可 mock 接口）

**Files:**
- Create: `internal/feishu/api.go`、`internal/feishu/client.go`
- Test: `internal/feishu/client_test.go`（httptest 假飞书）

**Interfaces:**
- Produces:

```go
// api.go —— 全部飞书 HTTP 交互收口到两个接口，sync/bind/publish 只依赖接口，测试用 fakeAPI
type bitableAPI interface {
	AppCreate(ctx context.Context, name string) (appToken string, err error)
	TableCreate(ctx context.Context, appToken, name string, fields []Field) (tableID string, err error)
	ViewCreate(ctx context.Context, appToken, tableID, name, viewType string) error
	RecordSearch(ctx context.Context, appToken, tableID string) ([]Record, error) // 内部分页聚合
	RecordCreate(ctx context.Context, appToken, tableID string, fields map[string]any) (recordID string, err error)
	RecordUpdate(ctx context.Context, appToken, tableID, recordID string, fields map[string]any) error
	DocCreate(ctx context.Context, folderToken, title string) (docToken string, err error)
	BlockAppend(ctx context.Context, docToken string, blocks []map[string]any) error
}
type Record struct {
	RecordID         string
	Fields           map[string]any
	LastModifiedTime int64 // 秒
}

// client.go
func NewClient(appID, appSecret, endpoint string) *Client // endpoint="" 时为 https://open.feishu.cn；测试注入 httptest URL
func (c *Client) API() bitableAPI
```

- `Client` 职责：tenant_access_token 获取/缓存/过期前 5 分钟刷新（POST `/open-apis/auth/v3/tenant_access_token/internal`）；所有请求过 `doWithRetry`（429 或 99991400 → 指数退避 500ms*2^n，最多 5 次）；`RecordCreate/RecordUpdate/RecordUpdate` 经 `singleFlight`（每 appToken 一把 mutex，即 spec 的"每 base 串行写"）
- SDK 说明：`api.go` 的生产实现基于 `larksuite/oapi-sdk-go/v3` 的对应 service；因 SDK 类型名可能随版本漂移，允许该文件改用 `net/http` + `encoding/json` 直连官方 REST（同样是官方 API），**接口签名不变**

- [ ] **Step 1: 失败测试**（httptest 假端点：token 端点返回固定 token；一个端点先 429 两次再 200，断言重试成功且共 3 次请求；两个并发 RecordCreate 断言串行——用 channel 记录进入/离开顺序无交叠）
- [ ] **Step 2: 确认失败 → Step 3: 实现 → Step 4: 通过 → Step 5: Commit** `git commit -am "feat: feishu client base (token cache, 429 backoff, per-base write serialization)"`

---

### Task 11: feishu bind（建 base/表/视图/文档）

**Files:**
- Create: `internal/feishu/bind.go`
- Modify: `internal/cli/feishu.go`（新增 `pulse feishu bind --project demo`）
- Test: `internal/feishu/bind_test.go`

**Interfaces:**
- Consumes: `bitableAPI`、`store.SaveProject`
- Produces:

```go
// Bind 为项目创建共享 base（任务表+版本表）与沉淀文档，token 写回项目记录（SaveProject）。
// 幂等：项目已有 bitable_app_token 时直接返回，不再创建。
func Bind(ctx context.Context, c *Client, s *store.Store, p model.Project) (model.Project, error)
```

- 任务表字段（spec §6 映射，select 选项随记录写入自动创建，无需预建）：`任务名(text)` `状态(text)` `负责人(text)` `优先级(text)` `开始(text)` `截止(text)` `预估人日(number)` `版本(text)` `已废弃(checkbox)` `updated_by(text)`
  ——**计划决策**：日期与单选一律用 text 字段落 Bitable（避免日期/单选类型在 API 写入时的格式约束，换取映射零歧义；甘特视图对文本日期列不可用时，用户在 Bitable 里把列改为日期类型即可，一次手动操作，bind 输出里提示）
- 版本表字段：`版本名(text)` `目标日期(text)` `状态(text)` `备注(text)`
- bind 后调 `ViewCreate(appToken, taskTableID, "甘特", "gantt")`，失败仅打警告不中断

- [ ] **Step 1: 失败测试**（fakeAPI 记录调用序列：AppCreate→TableCreate×2→ViewCreate→DocCreate→SaveProject；已绑定的项目再 Bind 不产生任何调用）
- [ ] **Step 2: 确认失败 → Step 3: 实现 → Step 4: 通过 → Step 5: CLI 接线 + Commit** `git commit -am "feat: feishu bind creates sync base and doc"`

---

### Task 12: sync 引擎（push/pull/自回声/软删）+ `pulse sync` + 自动 push

**Files:**
- Create: `internal/feishu/mapping.go`、`internal/feishu/sync.go`、`internal/feishu/autopush.go`
- Modify: `internal/cli/sync.go`（新增 `pulse sync --project demo`）；写命令（task/version add/update/rm）成功后调 `autopush.BestEffort`
- Test: `internal/feishu/sync_test.go`

**Interfaces:**
- Consumes: `bitableAPI`、tasks/versions store、`sync_state` 表
- Produces:

```go
type SyncResult struct {
	Pushed, Pulled, SkippedEcho, Deprecated int
	Conflicts                               []string // "task#12 本地有未同步修改，已被 Bitable 侧覆盖(LWW)"
}
func SyncProject(ctx context.Context, c *Client, s *store.Store, p model.Project, actor model.Member) (SyncResult, error)
func MarkSynced(s *store.Store, entity string, id int64, hash string) error         // 写 bitable_synced_hash
func GetSyncState(s *store.Store, key string) (string, bool, error)                 // sync_state 读写封装
func SetSyncState(s *store.Store, key, val string) error
func ContentHash(fields map[string]any) string                                      // canonical JSON(键排序) → sha256 前 16 位
```

- **push**（先于 pull）：对每个未 archived 的 task/version 计算 `ContentHash(mappedFields)`，若 ≠ `bitable_synced_hash` → record_id 为空则 RecordCreate（回填 record_id）否则 RecordUpdate；archived=1 且 record_id 非空且 synced_hash≠"deprecated" → RecordUpdate(已废弃=true) 并把 synced_hash 置 "deprecated"
- **pull**：RecordSearch 全量 → 记录 `max(last_modified_time)` 写 `sync_state["pull_watermark:<project>:tasks"]` → 对每条记录：
  1. fields 含 `已废弃=true` → 本地 `archived=1`，continue
  2. `last_modified_time < 水位(上一轮存储值)` → continue（只处理增量；首轮全量）
  3. 本地无该 record_id → 创建本地行（新记录不可能是自己回声，直接插入）
  4. 本地有 → `ContentHash(remoteMappedFields)` == 本地 `bitable_synced_hash` → **自回声，跳过**；否则：若本地 hash≠synced_hash（本地也有脏改）→ 记入 Conflicts 并记 activity(`action="sync_conflict"`，detail 存双方 JSON)；然后 **Bitable 值覆盖本地**（LWW），MarkSynced
- **映射**（mapping.go）：`TaskToFields(t, memberName)` / `FieldsToTask(f, local model.Task) (changed model.Task)`；负责人存成员名文本；status 优先级等直传文本
- **autopush**：`BestEffort(s, cfg, projectKey)`——未配置或未 bind 时静默返回；错误只打印 warning 到 stderr，绝不影响命令退出码

- [ ] **Step 1: 失败测试**（fakeAPI 脚本化，至少 6 例）：
  1. 本地新建 task → push 产生 RecordCreate 且 record_id 回填、hash 落库
  2. push 后立即 pull 同一条 → SkippedEcho=1（自回声）
  3. fake 返回远端修改过的记录 → 本地字段被覆盖、synced_hash 更新
  4. 本地脏改 + 远端同记录也改 → Conflicts 长度 1 且本地值=远端值
  5. 远端 已废弃=true → 本地 archived=1
  6. 本地 SoftDelete 后 sync → RecordUpdate(已废弃=true) 且本地 synced_hash="deprecated"
- [ ] **Step 2: 确认失败 → Step 3: 实现 mapping.go → sync.go → Step 4: 测试通过**
- [ ] **Step 5: `pulse sync` CLI**（执行顺序 pull 前的 push 按上述；输出 `已推送 N、拉取 M、跳过回声 K、废弃 J、冲突 C`；冲突逐行列出）+ 报表命令前 stale 检查（`GetSyncState("last_pull:<project>")` 超过 `sync.stale_minutes` 且已配置飞书 → 先静默 SyncProject）
- [ ] **Step 6: 写命令后 autopush 接线 → `go test ./...` → Commit** `git commit -am "feat: two-way sync with echo suppression, soft delete, LWW conflicts"`

---

### Task 13: publish（报表沉淀到飞书文档）+ feishu CLI 收口

**Files:**
- Create: `internal/feishu/publish.go`
- Modify: `internal/cli/feishu.go`（`pulse feishu publish --project demo --report weekly|versions|all`）
- Test: `internal/feishu/publish_test.go`

**Interfaces:**
- Consumes: `bitableAPI.DocCreate/BlockAppend`、reports 包
- Produces:

```go
// PublishReport 先 pull 同步（保证数据新鲜），生成报表并追加到项目绑定文档。
// 未绑定文档时自动 DocCreate 并写回项目。幂等性说明：每次 publish 追加新块（报表是时间线性质），
// 顶部写入形如 "## 周报 2026-W37（生成于 2026-09-15 08:00，由 codex 触发）" 的标题块防混淆。
func PublishReport(ctx context.Context, c *Client, s *store.Store, p model.Project,
	report string, actorName string) error
```

- 块构造（docx REST 约定，走 api.go 的 BlockAppend）：heading2=`{"block_type":4,...}`、text=`{"block_type":2,...}`、bullet=`{"block_type":12,...}`、todo=`{"block_type":17,...}`（实现时按官方文档核对 block_type 常量与内部字段名，测试断言只看"块序列的 block_type 顺序 + 文本内容"）
- weekly 的 Markdown 先转块序列（自带 30 行的 md→blocks 简易转换：`## `/`- `/`- [ ]`/普通行），不引第三方 md 库

- [ ] **Step 1: 失败测试**（fakeAPI 断言：块顺序=标题→正文 bullet；落款块含"由 pulse 导出 · 触发人 {actor}"；未绑定文档时先 DocCreate 且 SaveProject 回写）
- [ ] **Step 2: 确认失败 → Step 3: 实现 → Step 4: 通过 → Step 5: CLI + Commit** `git commit -am "feat: publish reports to feishu docx with attribution footer"`

---

### Task 14: E2E 手动验收清单 + README + 构建

**Files:**
- Create: `README.md`、`Makefile`
- Create: `docs/superpowers/plans/e2e-checklist.md`

**Interfaces:** 无代码接口；本任务交付可验收物。

- [ ] **Step 1: Makefile**

```makefile
BINARY := pulse
build:
	go build -o $(BINARY) .
test:
	go test ./...
vet: ; go vet ./...
```

- [ ] **Step 2: README.md**——安装（go install）、`PULSE_HOME` 说明、五分钟上手（init→member→task→version→report gantt→接 ZCode/CC/Codex 的 `.mcp.json` 配置示例）、飞书自建应用开通步骤（权限：bitable 读写、docx 读写、drive 文件创建；tenant token；secret 环境变量）、`.mcp.json` 示例：

```json
{
  "mcpServers": {
    "pulse": {
      "command": "/path/to/pulse",
      "args": ["mcp"],
      "env": { "PULSE_ACTOR": "codex", "PULSE_HOME": "/Users/me/.pulse" }
    }
  }
}
```

- [ ] **Step 3: E2E 手动清单**（写入 e2e-checklist.md，全部需在真实飞书测试租户执行）：
  1. `PULSE_HOME=/tmp/p1 pulse init demo` + 加成员/任务/版本，`report gantt` HTML 可打开
  2. `feishu bind` → 飞书出现 base（任务/版本表）+ 文档 + 甘特视图
  3. `pulse sync` → Bitable 出现记录；改 Bitable 一条状态 → 再 `sync` → 本地已变
  4. 双机：`PULSE_HOME=/tmp/p2`（另一"人"，default_actor 不同）bind 同一项目（bind 需支持指向既有 base：`--app-token` 参数，**Task 11 的 bind CLI 需带此参数**）→ 两侧互推互拉收敛
  5. MCP：ZCode/CC 挂 `.mcp.json`，让 agent `add_task`/`update_task`，检查 activity 中 actor_type=agent 且 on_behalf_of 正确
  6. `feishu publish --report weekly` → 文档出现周报块与归因落款
- [ ] **Step 4: `make test vet build` 全绿 → Commit** `git commit -am "docs: readme, e2e checklist, makefile"`

---

## Self-Review 记录

1. **Spec 覆盖**：§3 数据模型→Task 2-6（requirement_id 字段已预留但无接口，属 v1.1）；§3 同步元数据→Task 2/12；§4 报表+计算口径→Task 7/8；§5.1 CLI→Task 1/4/5/6/8/11/12/13；§5.2 MCP→Task 9；§6 飞书→Task 10-13；§7 技术栈→Global Constraints；§8 错误处理→Task 10(退避)/12(dirty 保留、缺字段告警在 FieldsToTask 中忽略未知字段并在 SyncResult 输出 warning——已含于 Step 1 例 3 的实现要求)/13；§9 测试→各任务 TDD 步骤 + Task 14 E2E。**缺口检查**：spec §6"pull 到缺字段记录跳过并告警"在 Task 12 测试例中未单列——实现时并入例 3 断言。
2. **占位符扫描**：Task 9 的 go-sdk API 名称与 Task 13 的 block_type 常量标注了"以官方文档核对"——这是对外部依赖版本漂移的显式处理指令，注册/断言逻辑本身是完整的，不属于 TBD。
3. **类型一致性**：`TaskChanges`/`TaskFilter`/`SyncResult`/`ContentHash` 在 Task 5/7/8/12 的 Consumes/Produces 间已对齐；`memberName` 在 Task 12 映射中需要 names 查询——实现时在 sync.go 内部 `ListMembers` 建 id→name map（不必改接口）。

